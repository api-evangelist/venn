generated: '2026-09-02'
method: generated
source: graphql/venn-tenant.graphql
note: >-
  Venn publishes no Agent Skills, no AGENTS.md and no llms.txt — a search of every
  Venn host found none. These three skills were generated by API Evangelist from
  the live GraphQL contract. Every operation, argument and input field named in
  them was verified against the introspected schema; nothing is invented. They are
  grounded operating instructions for surfaces Venn genuinely exposes, not a claim
  that Venn published them.
api: Venn Tenant GraphQL API
endpoint: https://api.venn.city/production/graphql
provider_published: false
skills:
  - name: venn-list-residents
    file: venn-list-residents.md
    summary: >-
      Page residents of a building or community through the mandatory cursor
      pagination, with the where/orderBy vocabulary and the soft-delete trap.
    operations:
      - Query.usersConnection
      - Query.membersConnection
      - Query.buildings
      - Query.hoods
    risk: read-only
  - name: venn-book-shared-space
    file: venn-book-shared-space.md
    summary: >-
      Check real availability, book a slot, verify it, and cancel it — the one flow
      with a named first-class reversal operation (and no published window for it).
    operations:
      - Query.sharedSpaces
      - Query.getTimeSlotsForDates
      - Query.checkIfTimeSlotsAvailable
      - Mutation.bookTimeSlots
      - Query.myBookedTimeSlots
      - Mutation.cancelBooking
    risk: reversible write
  - name: venn-broadcast-to-community
    file: venn-broadcast-to-community.md
    summary: >-
      Size the audience, draft, then send — and where Venn's single idempotencyKey
      protects you (per-user notifications) versus where it does not (broadcasts).
    operations:
      - Query.getBroadcastAudienceUserCount
      - Mutation.createBroadcast
      - Mutation.createNotification
      - Query.broadcasts
    risk: high-consequence write, not reversible after delivery
skill_count: 3
x-evidence:
  checked: '2026-09-02'
  evidence:
    - url: https://api.venn.city/production/graphql
      status: 200
    - url: https://venn.city/llms.txt
      status: 404
